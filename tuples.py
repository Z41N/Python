#tuples = an immutable list. Instead of using [] its just ()
dimensions = (100, 200)
print(dimensions[0])
print(dimensions[1])

for dimension in dimensions:
	print(dimension)

#This will give you an error:
#dimensions[0] = 250
#However, you can replace a tuple completely with new tuple, that is valid

	
				
